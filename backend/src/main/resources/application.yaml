spring:
  application:
    name: cashwise
  
  # ✅ H2 Database (Development) - ACTIVE
  datasource:
    url: jdbc:h2:mem:cashwise
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
      path: /h2-console
  
  # ❌ PostgreSQL (Production) - COMMENTED OUT
  # datasource:
  #   url: jdbc:postgresql://localhost:5432/cashwise
  #   username: admin
  #   password: 123
  #   driver-class-name: org.postgresql.Driver
  
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

jwt:
  secret: mySecretKeyForJWTTokenGenerationThatIsAtLeast256BitsLong
  expiration: 86400000
